<!--定义Mapper映射文件的约束和基本结构-->
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lyq.mapper.EmpMapper">
    <select id="fenyeGet" resultType="com.lyq.pojo.Emp">
        select e.*, d.name deptName from emp as e left join dept as d on e.dept_id = d.id
        <where>
            <if test="name != null and name != ''">
                e.name like concat('%',#{name},'%')
            </if>
            <if test="gender != null">
                and e.gender = #{gender}
            </if>
            <if test="begin != null and end != null">
                and e.entry_date between #{begin} and #{end}
            </if>
        </where>
    </select>

    <!--批量删除员工信息，open和close指定执行拼接语句的开始和结束-->
    <delete id="deleteByIds">
        delete from emp where id in
        <foreach collection="ids" item="id" open="(" close=")" separator=",">
            #{id}
        </foreach>
    </delete>

    <!--批量删除员工工作经历信息-->
    <delete id="deleteExprByIds">
        delete from emp_expr where emp_id in
        <foreach collection="ids" item="id" open="(" close=")" separator=",">
            #{id}
        </foreach>
    </delete>


    <!--自定义结果集ResultMap-->
    <!--    根据员工 ID，查询该员工的「完整基础信息」+ 关联的「所有工作经历信息」，-->
    <!--    并自动把数据库查询结果，封装到 Java 的实体类对象中，实现「数据库数据 → Java 对象」的无缝转换-->
    <resultMap id="resultMap" type="com.lyq.pojo.Emp">
<!--        select查询语句中resultMap="resultMap"- 最终要封装成 com.lyq.pojo.Emp 类型的对象-->
        <id column="id" property="id" />
        <result column="username" property="username" />
        <result column="password" property="password" />
        <result column="name" property="name" />
        <result column="gender" property="gender" />
        <result column="phone" property="phone" />
        <result column="job" property="job" />
        <result column="salary" property="salary" />
        <result column="image" property="image" />
        <result column="entry_date" property="entryDate" />
        <result column="dept_id" property="deptId" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />

        <!--封装empExprs-->
        <collection property="empExprs" ofType="com.lyq.pojo.EmpExpr">
<!--            type是主实体类型，ofType是集合元素类型-->
            <id column="ee_id" property="id"/>
            <result column="ee_company" property="company"/>
            <result column="ee_job" property="job"/>
            <result column="ee_begin" property="begin"/>
            <result column="ee_end" property="end"/>
            <result column="ee_empid" property="empId"/>
        </collection>
    </resultMap>

    <!--根据ID查询员工的详细信息-->
    <select id="getInfo" resultMap="resultMap">
        select e.*,
               ee.id ee_id,
               ee.emp_id ee_empid,
               ee.begin ee_begin,
               ee.end ee_end,
               ee.company ee_company,
               ee.job ee_job
        from emp e left join emp_expr ee on e.id = ee.emp_id
        where e.id = #{id}
    </select>
<!--    MyBatis 按照resultMap的规则，自动合并 + 封装数据，最终生成1 个 Emp 对象：-->
<!--    提取 SQL 结果中的员工基础字段（id/name/entry_date 等），封装到 Emp 对象的基础属性中；-->
<!--    提取 SQL 结果中的工作经历字段（ee_id/ee_company 等），每一条经历封装为 1 个empExprs对象；-->
<!--    将所有EmpExpr对象，统一存入 Emp 对象的empExprs集合(List)属性中；-->
<!--    最终返回：1 个完整的Emp对象（含基础信息 + empExprs 集合）。-->


    <!--根据ID更新员工信息-->
    <update id="updateEmp">
        update emp
        <set>
            <if test="username != null and username != ''">username = #{username},</if>
            <if test="password != null and password != ''">password = #{password},</if>
            <if test="name != null and name != ''">name = #{name},</if>
            <if test="gender != null">gender = #{gender},</if>
            <if test="phone != null and phone != ''">phone = #{phone},</if>
            <if test="job != null">job = #{job},</if>
            <if test="salary != null">salary = #{salary},</if>
            <if test="image != null and image != ''">image = #{image},</if>
            <if test="entryDate != null">entry_date = #{entryDate},</if>
            <if test="deptId != null">dept_id = #{deptId},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </set>
        where id = #{id}
    </update>

    <!-- 统计各个职位的员工人数 ，返回pos表示职位，total表示各个职位的数量-->
<!--    resultType="java.util.Map"：将每一行查询结果，自动封装为一个Map对象-->
    <select id="countEmpJobData" resultType="java.util.Map">
        select
            (case job when 1 then '班主任'
                      when 2 then '讲师'
                      when 3 then '学工主管'
                      when 4 then '教研主管'
                      when 5 then '咨询师'
                      else '其他' end)  pos,
            count(*) total
        from emp group by job
        order by total
    </select>

    <!-- 统计员工的性别信息 -->
    <select id="countEmpGenderData" resultType="java.util.Map">
        select
            if(gender=1,'男','女') as name,
            count(*) as value
            from emp group by gender ;
    </select>

<!--    根据部门ID，查询该部门的员工数量-->
    <select id="getNumByDeptId" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM emp WHERE dept_id = #{deptid}
    </select>
</mapper>